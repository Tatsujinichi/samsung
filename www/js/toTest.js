gl.readPixels(0, 0, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.readPixels(0, 0, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.readPixels(0, 0, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.lineWidth(lineWidth);
gl.uniform4fv(this._currentProgram.uniforms.uBbox, bbox);
gl.bindTexture(gl.TEXTURE_2D, textureHandle.element.binder);
gl.bindTexture(gl.TEXTURE_2D, textureHandle.element.binder);
gl.uniformMatrix4fv(this._currentProgram.uniforms.uMatrix, false, this._matrixStack[0]);
gl.drawArrays(drawMode, offset, nVertices);
gl.createBuffer();
gl.bindBuffer(gl.ARRAY_BUFFER, vertexBufferBinder);
gl.bindBuffer(gl.ARRAY_BUFFER, vertexBufferBinder);
gl.bufferData(gl.ARRAY_BUFFER, vertexBuffer, gl.STATIC_DRAW);
gl.bufferData(gl.ARRAY_BUFFER, vertexBuffer, gl.STATIC_DRAW);
gl.bufferData(gl.ARRAY_BUFFER, vertexBuffer, gl.STATIC_DRAW);
gl.bufferSubData(gl.ARRAY_BUFFER, startingByte, subBuffer);
gl.bufferSubData(gl.ARRAY_BUFFER, startingByte, subBuffer);
gl.blendFunc(gl.ONE, gl.ONE_MINUS_SRC_ALPHA);
gl.blendFunc(gl.ONE, gl.ONE_MINUS_SRC_ALPHA);
gl.blendFunc(gl.ONE, gl.ONE_MINUS_SRC_ALPHA);
gl.enable(gl.BLEND);
gl.enable(gl.BLEND);
gl.activeTexture(gl.TEXTURE0);
gl.activeTexture(gl.TEXTURE0);
gl.getParameter(gl.FRAMEBUFFER_BINDING);
gl.getParameter(gl.FRAMEBUFFER_BINDING);
gl.getParameter(gl.MAX_TEXTURE_SIZE);
gl.getParameter(gl.MAX_TEXTURE_SIZE);
gl.LINES,
gl.TRIANGLES
gl.clearColor(0.0, 0.0, 0.0, 0.0);
gl.clearColor(r, g, b, a);
gl.deleteTexture(textureData.binder);
gl.viewport(0, 0, width, height);
gl.enable(gl.BLEND);
gl.enable(gl.BLEND);
gl.disable(gl.BLEND);
gl.disable(gl.BLEND);
gl.uniformMatrix4fv(this._currentProgram.uniforms.uMatrix, false, matrix);
gl.bindTexture(this.gl.TEXTURE_2D, textureBinder);
gl.bindTexture(this.gl.TEXTURE_2D, textureBinder);
gl.drawArrays(this.gl.TRIANGLES, 0, 6);
gl.drawArrays(this.gl.TRIANGLES, 0, 6);
gl.clear(this.gl.COLOR_BUFFER_BIT);
gl.clear(this.gl.COLOR_BUFFER_BIT);
gl.vertexAttribPointer(this.attributes.aPosition, 2, this.gl.FLOAT, false, this.vertexSize, 0);
gl.vertexAttribPointer(this.attributes.aPosition, 2, this.gl.FLOAT, false, this.vertexSize, 0);
gl.vertexAttribPointer(this.attributes.aColorMul, 4, this.gl.BYTE,  true,  this.vertexSize, 2 * floatSize + 2 * shortSize);
gl.vertexAttribPointer(this.attributes.aColorMul, 4, this.gl.BYTE,  true,  this.vertexSize, 2 * floatSize + 2 * shortSize);
gl.vertexAttribPointer(this.attributes.aColorAdd, 4, this.gl.BYTE,  true,  this.vertexSize, 2 * floatSize + 2 * shortSize + 4 * byteSize);
gl.vertexAttribPointer(this.attributes.aColorAdd, 4, this.gl.BYTE,  true,  this.vertexSize, 2 * floatSize + 2 * shortSize + 4 * byteSize);
gl.vertexAttribPointer(this.attributes.aPosition, 2, this.gl.FLOAT,          false, this.vertexSize, 0);
gl.vertexAttribPointer(this.attributes.aPosition, 2, this.gl.FLOAT,          false, this.vertexSize, 0);
gl.vertexAttribPointer(this.attributes.aTexCoord, 2, this.gl.UNSIGNED_SHORT, true,  this.vertexSize, 2 * floatSize);
gl.vertexAttribPointer(this.attributes.aTexCoord, 2, this.gl.UNSIGNED_SHORT, true,  this.vertexSize, 2 * floatSize);
gl.vertexAttribPointer(this.attributes.aColorMul, 4, this.gl.BYTE,           true,  this.vertexSize, 2 * floatSize + 2 * shortSize);
gl.vertexAttribPointer(this.attributes.aColorMul, 4, this.gl.BYTE,           true,  this.vertexSize, 2 * floatSize + 2 * shortSize);
gl.vertexAttribPointer(this.attributes.aColorAdd, 4, this.gl.BYTE,           true,  this.vertexSize, 2 * floatSize + 2 * shortSize + 4 * byteSize);
gl.vertexAttribPointer(this.attributes.aColorAdd, 4, this.gl.BYTE,           true,  this.vertexSize, 2 * floatSize + 2 * shortSize + 4 * byteSize);
gl.createShader(gl.FRAGMENT_SHADER);
gl.createShader(gl.FRAGMENT_SHADER);
gl.createShader(gl.VERTEX_SHADER);
gl.createShader(gl.VERTEX_SHADER);
gl.shaderSource(shader, shaderData.script);
gl.compileShader(shader);
gl.getShaderParameter(shader, gl.COMPILE_STATUS)) {
gl.getShaderParameter(shader, gl.COMPILE_STATUS)) {
gl.getShaderInfoLog(shader));
gl.createProgram();
gl.attachShader(programBinder, this._getShader(program.vertexShaderId));
gl.attachShader(programBinder, this._getShader(program.fragmentShaderId));
gl.linkProgram(programBinder);
gl.getProgramParameter(programBinder, gl.LINK_STATUS)) {
gl.getProgramParameter(programBinder, gl.LINK_STATUS)) {
gl.getProgramInfoLog(programBinder);
gl.deleteProgram(programBinder);
gl.useProgram(programBinder);
gl.getAttribLocation(programBinder, attributeId);
gl.getUniformLocation(programBinder, uniformId);
gl.uniform1i(uniformLocation, textureIndexes[uniformId]);
gl.disableVertexAttribArray(a);
gl.enableVertexAttribArray(a);
gl.useProgram(program.binder);
gl.activeTexture(gl.TEXTURE1);
gl.activeTexture(gl.TEXTURE1);
gl.bindTexture(gl.TEXTURE_2D, data.mask);
gl.bindTexture(gl.TEXTURE_2D, data.mask);
gl.activeTexture(gl.TEXTURE0);
gl.activeTexture(gl.TEXTURE0);
gl.uniform1f(this._currentProgram.uniforms.uRatio, data.ratio);
gl.uniform1f(this._currentProgram.uniforms.uResolution, data.resolution);
gl.disable(gl.SCISSOR_TEST);
gl.disable(gl.SCISSOR_TEST);
gl.enable(gl.SCISSOR_TEST);
gl.enable(gl.SCISSOR_TEST);
gl.scissor(scissor[0], scissor[1], scissor[2], scissor[3]);
gl.createTexture();
gl.createFramebuffer();
gl.bindTexture(gl.TEXTURE_2D, textureBinder);
gl.bindTexture(gl.TEXTURE_2D, textureBinder);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, w, h, 0, gl.RGBA, gl.UNSIGNED_BYTE, null);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, w, h, 0, gl.RGBA, gl.UNSIGNED_BYTE, null);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, w, h, 0, gl.RGBA, gl.UNSIGNED_BYTE, null);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, w, h, 0, gl.RGBA, gl.UNSIGNED_BYTE, null);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, w, h, 0, gl.RGBA, gl.UNSIGNED_BYTE, null);
gl.bindFramebuffer(gl.FRAMEBUFFER, frameBufferBinder);
gl.bindFramebuffer(gl.FRAMEBUFFER, frameBufferBinder);
gl.framebufferTexture2D(gl.FRAMEBUFFER, gl.COLOR_ATTACHMENT0, gl.TEXTURE_2D, textureBinder, 0);
gl.framebufferTexture2D(gl.FRAMEBUFFER, gl.COLOR_ATTACHMENT0, gl.TEXTURE_2D, textureBinder, 0);
gl.framebufferTexture2D(gl.FRAMEBUFFER, gl.COLOR_ATTACHMENT0, gl.TEXTURE_2D, textureBinder, 0);
gl.framebufferTexture2D(gl.FRAMEBUFFER, gl.COLOR_ATTACHMENT0, gl.TEXTURE_2D, textureBinder, 0);
gl.bindFramebuffer(gl.FRAMEBUFFER, this._renderTarget.frameBufferBinder);
gl.bindFramebuffer(gl.FRAMEBUFFER, this._renderTarget.frameBufferBinder);
gl.bindFramebuffer(gl.FRAMEBUFFER, renderTarget.frameBufferBinder);
gl.bindFramebuffer(gl.FRAMEBUFFER, renderTarget.frameBufferBinder);
gl.viewport(0, 0, renderTarget.width, renderTarget.height);
gl.clear(gl.COLOR_BUFFER_BIT);
gl.clear(gl.COLOR_BUFFER_BIT);
gl.deleteFramebuffer(renderTarget.frameBufferBinder);
gl.readPixels(0, 0, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.readPixels(0, 0, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.readPixels(0, 0, w, h, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.LINEAR;
gl.LINEAR;
gl.NEAREST;
gl.LINEAR_MIPMAP_LINEAR;
gl.generateMipmap(gl.TEXTURE_2D);
gl.generateMipmap(gl.TEXTURE_2D);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, minFilter);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, minFilter);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, minFilter);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, magFilter);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, magFilter);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MAG_FILTER, magFilter);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);
gl.createTexture();
gl.bindTexture(gl.TEXTURE_2D, textureBinder);
gl.bindTexture(gl.TEXTURE_2D, textureBinder);
gl.pixelStorei(gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL, true);
gl.pixelStorei(gl.UNPACK_PREMULTIPLY_ALPHA_WEBGL, true);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, imageData);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, imageData);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, imageData);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, imageData);
gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, imageData);
gl.readPixels(0, CANVAS_HEIGHT - hh, ww, hh, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.readPixels(0, CANVAS_HEIGHT - hh, ww, hh, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.readPixels(0, CANVAS_HEIGHT - hh, ww, hh, gl.RGBA, gl.UNSIGNED_BYTE, pixels);
gl.vertexAttribPointer(this.attributes.aTexCoord, 2, this.gl.UNSIGNED_SHORT, true,  this.vertexSize, 2 * floatSize);
gl.vertexAttribPointer(this.attributes.aTexCoord, 2, this.gl.UNSIGNED_SHORT, true,  this.vertexSize, 2 * floatSize);
